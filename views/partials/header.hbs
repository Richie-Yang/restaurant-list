<header>
  <nav class="navbar navbar-light bg-light">
    <div class="container justify-content-between">
      <a class="navbar-brand" href="/">
        <img src="https://assets-lighthouse.s3.amazonaws.com/uploads/image/file/6227/restaurant-list-logo.png" width="30"
          height="30" class="d-inline-block align-top" alt="我的餐廳清單">
        {{#if isAuthenticated}}{{user.name}}{{else}}我{{/if}}的餐廳清單
      </a>

      <button class="navbar-toggler d-md-none" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse d-md-none" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          {{#if isAuthenticated}}
          <li class="nav-item">
            <a class="nav-link" href="/restaurants/new">新增餐廳</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/users/logout">登出</a>
          </li>
          {{else}}
          <li class="nav-item">
            <a class="nav-link" href="/users/login">登入</a>
          </li>
          {{/if}}
        </ul>
      </div>

      <div class="d-none d-md-block">
        {{#if isAuthenticated}}
        <a class="btn btn-outline-info" href="/restaurants/new">新增餐廳</a>
        <a class="btn btn-outline-info" href="/users/logout">登出</a>
        {{else}}
        <a class="btn btn-outline-info" href="/users/login">登入</a>
        {{/if}}
      </div>
    </div>
  </nav>
  <div class="banner"></div>
</header>